import { RegisterPageClient } from './register-page-client';

export const metadata = { title: 'Crear cuenta — AlquilaYa' };

export default function RegisterPage() {
  return <RegisterPageClient />;
}
