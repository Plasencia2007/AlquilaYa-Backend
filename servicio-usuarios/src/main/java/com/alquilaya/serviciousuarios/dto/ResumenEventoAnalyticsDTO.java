package com.alquilaya.serviciousuarios.dto;

/** Conteo de eventos de un tipo dentro de un rango de fechas (panel admin, ítem 455). */
public record ResumenEventoAnalyticsDTO(String evento, Long total) {
}
