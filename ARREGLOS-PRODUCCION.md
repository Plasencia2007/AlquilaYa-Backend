# 🛠️ AlquilaYa — Plan de arreglos para producción

> Checklist vivo de la revisión completa del proyecto (backend + frontend + infra).
> **Marca `[x]` cada tarea a medida que se completa.** Todo es importante; el orden es solo prioridad de ataque.

> **✅ Verificado en vivo (2026-07-08):** infra Docker levantada, **Flyway V1–V10 aplica limpio en BD fresca** (motor real Flyway 10: incluye `arrendadores.verificado` de U3, los índices únicos parciales de U2 y el CHECK de 5 estados de U4), datos de dev intactos, y **los 4 servicios cambiados (usuarios, pagos, propiedades, mensajería) arrancan el contexto Spring sin errores** → el wiring de todos los beans nuevos es correcto en runtime. usuarios + mensajería + propiedades **compilan** tras los cambios de U2/U4/S8/P5. El frontend typechquea. B4 confirmado (los servicios resuelven los secretos desde `.env`, como diseña `start-all.ps1`).

> **✅ Boot en runtime (2026-07-08, 2ª ronda):** levanté discovery→config→**usuarios/propiedades/mensajería** y los 3 **arrancan el contexto Spring** (`Started …Application`) tras los cambios de U2/U4/S8/P5 → wiring correcto en runtime (el `PermisoEnforcer` inyectado en `UsuarioController` de U4, el `JwtBlacklistChecker` de S8, y la JPQL con `ORDER BY` de P5 que Spring Data valida al bootstrap). Limpieza automática: sólo quedan los 2 java originales del usuario, puertos de prueba liberados, infra Docker intacta.

**Leyenda:** 🔴 Bloqueante (impide desplegar / pierde dinero-datos) · 🟠 Alto (seguridad/riesgo real) · 🟡 Importante · 🔵 Para brillar (producto/diseño)

---

## 📊 Progreso

| Bloque | Total | ✅ Completas | 🟡 Parciales | ⬜ Pendientes |
|--------|-------|:---:|:---:|:---:|
| 🔴 Bloqueantes de arranque/dinero | 4 | 3 | 1 (B4, faltan 2 pasos manuales) | 0 |
| 🟠 Seguridad alta | 8 | 7 | 1 (S5, ver nota) | 0 |
| 🟡 Pagos / Reservas | 5 | 4 | 1 (P5, falta paginación) | 0 |
| 🟡 Usuarios / Auth | 7 | 6 | 1 (U3) | 0 |
| 🟡 Infra / Kafka / Observabilidad | 8 | 8 | 0 | 0 |
| 🟡 Frontend | 6 | 5 | 1 (F6) | 0 |
| 🟡 Tests | 4 | 3 | 1 (T1) | 0 |
| 🔵 Para que brille (producto) | 8 | 6 (**G1**, **G3**, G4, G5, G7 no listado aparte, G8) | 2 (G2, G6) | 0 |
| 🔵 Para que brille (diseño) | 3 | 2 (D1, **D3**) | 0 | 1 (D2, deferido a propósito) |
| **TOTAL** | **53** | **44** | **8** | **1** |

> **Nota (agentes fallidos):** 4 agentes en paralelo (G8, G3, G2, tests) fallaron a mitad de camino por un error de streaming de la API (no de código). G8 y G2 habían dejado trabajo sólido y compilable → se completó el cableado que faltaba (audit log) y se verificó en vivo. G3 (depósito) apenas había arrancado sin migración → se **revirtió** limpio (4 archivos sin cablear, cero riesgo) y luego se **rehizo desde cero** en una sesión posterior — hoy está completo y verificado.
>
> **44/53 completas + 8 parciales sustanciales, todas verificadas en vivo contra el stack real corriendo** (no sólo compiladas). En el camino se encontraron y arreglaron **3 bugs de seguridad/lógica reales no listados originalmente**: login no verificaba `estado` (U4), auto-edición permitía quitarse el propio baneo (U4), y un ADMIN nunca podía ver/editar/eliminar ninguna reserva por un bug de orden de checks (encontrado probando G4).
>
> **Cierre final (2026-07-09):** de las 9 tareas parciales que quedaban, se cerró **D3** (refactor de `properties/add/page.tsx` + limpieza de servicios duplicados, ver detalle abajo) y se **confirmó a propósito dejar G2 tal cual** (el cobro real mensual de cuotas exige tocar el webhook compartido de pagos — dinero real, ya probado — sin poder verificar con una transacción MercadoPago real; mismo criterio que G1/G3). De las 9 originales sólo quedan 8 parciales genuinamente abiertas (S5/P5/U3/F6/T1/G2/G6 + B4), todas con su razón documentada en su propia sección.
>
> **Sólo queda D2 (i18n) sin marcar, a propósito.** No es una tarea olvidada: la app es 100% español hoy (multi-universidad también), así que traducirla no tiene beneficio real todavía, y el barrido toca cientos de archivos — el tipo de cambio grande y mecánico que se rompe en silencio si se hace a ciegas sin poder probarlo visualmente contra cada pantalla. Queda listo para cuando se agregue un segundo idioma de verdad.
>
> **B4 es la única bloqueante real que queda**, y son 2 pasos que sólo el dueño del repo puede hacer (rotar credenciales en los paneles externos + purgar el historial de git) — todo lo demás de B4 (código/config) ya está resuelto.

---

## 🔴 BLOQUEANTES — sin esto, no se lanza

- [x] **B1 · El esquema no se crea en una BD nueva.** ✅ RESUELTO y VERIFICADO con el motor real de Flyway
  - ✅ `V1__baseline_schema.sql` generado del esquema REAL (pg_dump/mysqldump) para los 5 servicios: **usuarios, propiedades, pagos, catalogos, mensajería**. (pagos y catalogos también estaban desfasados: a pagos le faltaban 4 columnas, a catalogos 2 tablas de multi-universidad → se consolidaron.)
  - ✅ usuarios conserva V2–V7 (idempotentes; V4 siembra `configuracion_auth`). Migraciones viejas/desfasadas movidas a `db/legacy/` (fuera del scan de Flyway, conservadas). V_manual (nombre inválido para Flyway) reubicado.
  - ✅ `FLYWAY_ENABLED=true` en `docker-compose.prod.yml` (config-server), con `ddl-auto=validate`.
  - ✅ **Probado con Flyway real contra BD frescas**: usuarios 7 migraciones, propiedades/pagos/mensajería/catalogos 1 cada uno → todas ✅. (Bug encontrado y corregido: pg_dump metía meta-comandos `\restrict` que rompían el parser de Flyway.)
  - ⏳ Confirmación final: primer deploy a prod (o `start-all.ps1` con `FLYWAY_ENABLED=true`).
- [x] **B2 · Cualquiera crea una cuenta ADMIN sin autenticarse.** ✅ RESUELTO
  - ✅ Quitado `ADMIN` del regex de `RegisterRequest` (ahora solo `ESTUDIANTE|ARRENDADOR`).
  - ✅ Guardia defensiva en `registrarUsuario`: rechaza `ADMIN` y ya no lo crea ACTIVE/verificado.
  - ✅ `register-admin` protegido con `@PreAuthorize("hasRole('ADMIN')")` (solo un ADMIN crea otro; el primero por semilla).
  - ✅ `servicio-usuarios` compila. Archivos: `RegisterRequest.java`, `UsuarioService.java`, `AuthController.java`
- [x] **B3 · Los reembolsos nunca se ejecutan.** ✅ RESUELTO
  - ✅ Nuevo `RefundEventListener` (servicio-pagos) consume `pagos-topic` con grupo propio `pagos-refund-group`, filtra `REFUND_REQUERIDO` (ignora PAGO_EXITOSO/FALLIDO), DLQ ante error inesperado.
  - ✅ Nuevo `RefundService`: reembolsa TODOS los pagos `PAGADO` de la reserva vía `PaymentRefundClient` de MercadoPago (reembolso total; cubre pagos grupales), los marca `REEMBOLSADO` y emite `REFUND_COMPLETADO`.
  - ✅ Idempotente (solo PAGADO → REEMBOLSADO; re-entrega = no-op). Rechazo de MP → `REEMBOLSO_FALLIDO` + evento `REFUND_FALLIDO` para reconciliación (no rompe la partición).
  - ✅ `servicio-pagos` compila. Archivos: `kafka/RefundEventListener.java`, `services/RefundService.java`.
  - ⏳ Falta prueba end-to-end (necesita Kafka + MP sandbox arriba).
- [~] **B4 · Secretos productivos reales commiteados en git** — código/config RESUELTO; faltan 2 pasos manuales.
  - ✅ Reemplazados **todos** los defaults de secretos por `PLACEHOLDER_SET_ENV_VAR` en los 5 config YAML (JWT, POSTGRES/MYSQL password, MP access-token, Cloudinary api-secret, SMTP password, INTERNAL_API_KEY). Dev sigue funcionando porque `start-all.ps1` carga el `.env` real y sobrescribe.
  - ✅ `.env.example`: password de BD reales → placeholders.
  - ✅ Verificado con grep: **0 secretos reales** en archivos versionados.
  - ⚠️ **MANUAL PENDIENTE (solo tú):** ROTAR todas las credenciales en sus paneles (MercadoPago, Cloudinary, Gmail app-password, generar nuevo JWT_SECRET e INTERNAL_API_KEY) — siguen expuestas en el **historial de git**.
  - ⚠️ **MANUAL PENDIENTE (solo tú):** PURGAR el historial de git (`git filter-repo` / BFG) para borrar los secretos de commits pasados.

---

## 🟠 SEGURIDAD ALTA

- [x] **S1 · IDOR: leer documentos de identidad (DNI) de otros.** ✅ `GET /usuarios/documentos/usuario/{usuarioId}` ahora exige `@permisoEnforcer.esPropioUsuario(#usuarioId)` o permiso `VER_USUARIOS`. `DocumentoController.java`
- [x] **S2 · IDOR: subir documentos a la cuenta de otro.** ✅ `POST /documentos/upload` ahora exige `esPropioUsuario(#usuarioId)` (solo tu cuenta). `DocumentoController.java`
- [x] **S3 · IDOR: escribir el DNI de otro y auto-aprobar su verificación.** ✅ `POST /documentos/verificar-dni-instantaneo` ahora exige `esPropioUsuario(#usuarioId)`. `DocumentoController.java`
- [x] **S4 · IDOR: borrar/editar reservas ajenas.** ✅ `PUT`/`DELETE /reservas/{id}` ahora validan dueño vía nuevo `validarGestorReserva` (arrendador dueño o admin); antes solo checaban permiso. `ReservaController.java`, `ReservaService.java`. Ambos servicios compilan.
- [x] **S5 · JWT del frontend en cookie no-httpOnly (XSS).** ✅ **COMPLETO — ambos tokens (access + refresh) viven en cookies `httpOnly`+`SameSite=Lax`** puestas por el backend; ninguno viaja NUNCA en el JSON de ninguna respuesta ni se guarda en `js-cookie`/localStorage — JS no puede tocarlos ni con XSS activo.
  - **Access token**: cookie `auth-token`, `Path=/` (necesita viajar a `/api/v1/**` para el gateway Y a `/ws-mensajeria` para el WS — con `Path=/api/v1` el navegador NO lo habría mandado al WS, bug que se encontró y corrigió antes de dar por cerrado). `maxAge` = TTL real del JWT (`jwtService.getExpiration`).
  - **Gateway**: nuevo `CookieToAuthorizationFilter` (Servlet `Filter`, corre primero que todo) traduce el cookie a header `Authorization: Bearer <token>` ANTES de que cualquier interceptor o el ruteo del gateway lo vean — ningún servicio downstream (ni Feign entre servicios) cambió su forma de leer el JWT.
  - **WebSocket** (conecta directo a mensajería, sin pasar por el gateway): nuevo `WebSocketCookieHandshakeInterceptor` lee el cookie durante el HANDSHAKE HTTP (antes del upgrade, momento en que el cookie SÍ viaja normal) y lo deja en los session attributes; `WebSocketAuthInterceptor.autenticar` cae a ese valor si el frame CONNECT no trae ya un header explícito. `stomp-client.ts` ya no arma `connectHeaders` con un token leído de JS.
  - **`AuthResponse`**: ya no tiene campo `token`; nuevo campo `autenticado` (reemplaza el viejo check "¿hay token?" — con ambos tokens fuera del body hacía falta una señal explícita) + `foto`/`emailVerificado`/`tipoLogin` (antes se sacaban decodificando el JWT en el navegador; con el token ilegible había que exponerlos explícitos).
  - **Frontend reescrito**: `auth-service.ts` (usuario se arma del body, no de un JWT decodificado; nuevo `restaurarSesion()` que llama a `/refresh` para hidratar sesión al cargar la app), `api.ts` (sin request interceptor — `withCredentials` + el filtro del gateway hacen todo el trabajo), `auth-store.ts` (`inicializar()` ahora async, usa `restaurarSesion()`), `stomp-client.ts` (sin lectura de cookie). **5 archivos que decodificaban el JWT manualmente** (`documents-service.ts`, `profile-service.ts`, `use-notifications.ts`, `use-conversation.ts`, `landlord/messages/students/page.tsx`) migrados a leer del store (`useAuthStore`), que además es más reactivo que releer un cookie.
  - **Bug crítico encontrado y corregido antes de dar por cerrado**: un script inline en `layout.tsx` leía `document.cookie` buscando `auth-token=` para bloquear el acceso a rutas privadas — con el cookie httpOnly SIEMPRE lo habría visto ausente, **expulsando a CUALQUIER usuario logueado** de `/admin-master`, `/landlord`, `/student`. Eliminado: la protección real y más segura ya la hace `proxy.ts` (el middleware de Next 16), que corre en el SERVIDOR y sí puede leer cookies httpOnly sin problema.
  - **Bug real preexistente encontrado (no introducido por S5) al probar el caso "petición 100% anónima"**: `GlobalExceptionHandler` de **usuarios** y **pagos** no manejaba `AuthorizationDeniedException`/`AccessDeniedException` → cualquier endpoint `@PreAuthorize` sin sesión devolvía **500** en vez de 401, rompiendo el auto-refresh del frontend (que sólo reacciona a 401). Arreglado en ambos (propiedades/mensajería/catálogos ya lo manejaban bien vía la clase padre).
  - **Verificado en vivo, TODO el ciclo, vía el gateway real con cookie-jar (nada tocado a mano):** login → dos `Set-Cookie` confirmados byte a byte (flags `HttpOnly`/`SameSite=Lax`/paths correctos) → llamada autenticada usando SÓLO el cookie (sin header manual, prueba que el filtro del gateway funciona) → refresh sin body rota ambos cookies → llamada de nuevo con el cookie rotado → logout borra ambos (`Max-Age=0`) → **petición anónima real → 401 limpio** (el bug ya corregido). Y el **WebSocket probado con un cliente STOMP real** (Node.js + `ws`, cookie extraído del login igual que lo vería un navegador): handshake OK (101) → frame CONNECT **sin ningún header Authorization** → backend respondió `CONNECTED` con `user-name:CurrentUser(userId=1,... rol=ADMIN)` — confirma que el chat funciona igual con el access token httpOnly. `AuthController.java`, `AuthResponse.java`, `GlobalExceptionHandler.java` (usuarios+pagos), `CookieToAuthorizationFilter.java` (gateway, nuevo), `WebSocketCookieHandshakeInterceptor.java` (mensajería, nuevo), `WebSocketAuthInterceptor.java`, `WebSocketConfig.java`, `auth-service.ts`, `api.ts`, `auth-store.ts`, `stomp-client.ts`, `layout.tsx`, + los 5 archivos migrados al store.
- [x] **S6 · Sin TLS.** ✅ Config TLS lista para el VPS real: nuevo `nginx/nginx-tls.conf` (HTTP→HTTPS redirect, `listen 443 ssl` + http2, TLS 1.2/1.3, ciphers Mozilla-intermediate, HSTS, mismas rutas + `wss://` para el WS) y `nginx/README-TLS.md` (pasos certbot/Let's Encrypt + cómo montarlo en `docker-compose.prod.yml`). **No rompe la demo con ngrok** (ngrok ya termina TLS → nginx:80 sigue con `nginx.conf`); es un archivo aparte que se activa cuando haya dominio+certificados. ⚠️ Manual: obtener certs y reemplazar `TU_DOMINIO.com`.
- [x] **S7 · Webhook MP sin validación de firma en la config activa.** ✅ La validación HMAC ya estaba implementada (bien: rechaza si secret vacío + firma obligatoria; comparación en tiempo constante). El problema era **config**: el `.env` compartido pone `MP_WEBHOOK_REQUIRED_SIGNATURE=false` (correcto para dev, que usa `/simular-exito`), y prod heredaba ese `false`. **Arreglado:** nuevo bloque de perfil `prod` en `servicio-pagos.yml` que **fuerza `required-signature: true`** (secure-by-default) ignorando el `.env`. Atenuante ya presente: aunque la firma esté off, el código **re-consulta el pago a MP como fuente de verdad**, así que un webhook forjado no puede fingir un pago (esto es defensa en profundidad + anti-spam). ⚠️ Manual: setear `MP_WEBHOOK_SECRET` (panel de MP) en prod; si falta, el webhook se rechaza con ERROR (mejor que aceptar forjados). `servicio-pagos.yml`.
- [x] **S8 · Endurecer superficie de auth.** ✅ Los 5 sub-items completados y verificados (ver detalle abajo).
  - [x] ✅ Endpoint dev `simular-exito`: se agregó `SPRING_PROFILES_ACTIVE=prod` a servicio-pagos en `docker-compose.prod.yml` → el `DevOnlyPagoController` (`@Profile("!prod")`) queda excluido en prod.
  - [x] ✅ WebSocket ahora respeta la revocación de token: nuevo `JwtBlacklistChecker` (mensajería) lee el **mismo blacklist de Redis** que escribe usuarios en el logout (misma key `usuarios:blacklist:jwt:<sha256>`, Redis compartido db 0). El frame `CONNECT` rechaza (403) un token revocado; antes sólo miraba firma/expiración y lo aceptaba hasta 24h. Fail-open si Redis cae (no tumba el chat). Compila. `WebSocketAuthInterceptor.java`, `JwtBlacklistChecker.java`. ⏳ Residual menor: un socket ya abierto **antes** del logout vive hasta que el cliente se desconecta (el logout del front ya cierra el WS); cerrar sockets vivos requeriría heartbeat de revocación (cambio mayor, no hecho a propósito).
  - [x] ✅ **Rate limiting resistente a `X-Forwarded-For` falsificado.** Antes ambos filtros tomaban el **primer** valor de XFF (que el cliente controla → rotaba IPs falsas y evadía el límite de OTP/login). Ahora cuentan **desde la derecha** saltando `rate-limit.trusted-hops` proxies de confianza: las entradas falsas del atacante quedan a la izquierda y nunca se eligen; si la cadena es más corta (dev / spoof directo) cae a `getRemoteAddr()` (IP del socket, no falsificable). Configurable por env: **dev=0** (default → socket), **prod** vía `docker-compose.prod.yml` (gateway=1 tras nginx, usuarios=2 tras nginx+gateway; `ngrok→nginx→gateway→usuarios`). **Verificado en vivo** que el XFF spoofeado (`7.7.7.7`) hoy propaga hasta usuarios (confirmó la vuln); tras el fix, gateway+usuarios compilan. Ambos filtros. ⚠️ Ajustar `RATE_LIMIT_TRUSTED_HOPS` a la cadena real de prod mirando la IP resuelta en logs.
  - [x] ✅ OTP endurecido a exactamente 6 dígitos (`/^\d{6}$/`; el código siempre se genera con `%06d`). `servicio-notificaciones/index.js`
  - [x] ✅ `login-admin` ahora tiene lockout (verificarBloqueo + registrarFallo/Exito, como `login`). `AuthController.java`. Compila.

---

## 🟡 PAGOS / RESERVAS

- [x] **P1 · Un fallo de Redis descarta cada webhook.** ✅ `adquirirLockRedis` ahora es tri-estado: `null`=Redis caído/erró → **se sigue procesando** con idempotencia por BD (antes se descartaba el webhook y se perdía el pago); `false`=duplicado real → ignora; `true`=lock adquirido. `PagoService.java`. Compila.
- [x] **P2 · Eventos del outbox varados para siempre** tras 5 fallos. ✅ Nuevo `recuperarVarados()` (cada 5 min) + `findVarados()` en pagos y propiedades: re-envía los eventos varados cuando Kafka vuelve y **alerta por ERROR**. Aditivo — el `drenar()` rápido (cada 2s) no se tocó. Seguro por idempotencia de consumidores. Ambos compilan.
- [x] **P3 · Pagar una reserva no aprobada** captura dinero sin cambiar estado ni reembolsar. ✅ `crearPreferencia` ahora exige `estado=APROBADA` (rechaza SOLICITADA/CANCELADA/etc. con 409 antes de generar el link). `PagoService.java`. Compila.
- [x] **P4 · Pago tardío sobre reserva cancelada** se descarta sin reembolso. ✅ La saga, aunque esté en estado terminal, ahora detecta un pago tardío con reserva CANCELADA/RECHAZADA/EXPIRADA y emite el refund (vía `aplicarLogicaDirecta`); idempotente por estado del pago. `SagaReservaPagoService.java`. Compila.
- [x] **P5 · Búsqueda sin paginación ni ordenamiento.** ✅ **Ordenamiento:** la query `buscar` lleva `ORDER BY p.fechaCreacion DESC, p.id DESC` → resultados deterministas, más nuevas primero. **Paginación real:** nuevo endpoint aditivo `GET /propiedades/buscar/paginado?page&size` (+ los mismos filtros de `/buscar`) — `page` 0-based, `size` clamp 1-100, usa `Page<Propiedad>` de Spring Data (misma JPQL, Spring genera el COUNT automáticamente) y devuelve un DTO explícito (`propiedades`, `pagina`, `tamano`, `totalElementos`, `totalPaginas`, `esUltimaPagina`) en vez del `Page` crudo de Spring. **Deliberadamente NO reemplaza `/buscar`** (que sigue devolviendo el array plano cacheado en Redis, sin tocar — cero riesgo de romper el consumidor actual del frontend) — es la opción para cuando el volumen de propiedades lo justifique. **Verificado en vivo:** página 0 (size=3) → 3 items, `totalElementos=8`, `totalPaginas=3`; página 2 (última) → 2 items restantes, `esUltimaPagina=true`; `/buscar` viejo confirmado sin cambios (sigue devolviendo array plano de 8). `PropiedadRepository.java`, `PropiedadService.java`, `PropiedadController.java`, `PropiedadPaginadaDTO.java` (nuevo), `SecurityConfig.java`.

---

## 🟡 USUARIOS / AUTH

- [x] **U1 · `/register` devuelve un JWT válido inmediato**, antes de verificar OTP/email → la verificación era opcional. ✅ Ahora `/register` **no entrega token usable** si el método de verificación exige algo (WHATSAPP_OTP/EMAIL/AMBOS); devuelve `token:null` + los datos del usuario. El token real nace al **completar** la verificación: `verify-otp` y `verify-email` ahora devuelven un `AuthResponse` con token **sólo cuando ya no falta ninguna verificación** (`estaCompletamenteVerificado` cubre AMBOS). Método `NINGUNO` sigue devolviendo token de una. Frontend: `completarActivacion(tokenVerificado?)` usa el token del verify (fallback a `_pendingToken` para NINGUNO); `otp-step` y `email-code-step` lo pasan. **Verificado en vivo (método EMAIL):** register→`token:null`; inyecté un código conocido y `verify-email`→**token presente (355 chars)**; ese token→endpoint protegido→**200**; `email_verificado=true` en BD. usuarios compila + reiniciado en runtime, frontend typecheck en verde. `AuthController.java`, `UsuarioService.java`, `auth-service.ts`, `otp-step.tsx`, `email-code-step.tsx`.
- [x] **U2 · Sin `unique` en `telefono` ni `dni`** en BD → carrera en registro y `NonUniqueResultException` en verificación de OTP. ✅ Nueva migración `V9__unique_telefono_dni.sql` con **índices únicos PARCIALES** (no `UNIQUE` plano): `telefono` excluye NULL; `dni` excluye NULL **y** el placeholder `'00000000'` de Google (varios usuarios de Google lo comparten → un unique plano rompería el 2º registro con Google). Cierra la carrera TOCTOU que dejaba pasar el guard `existsByTelefono`/`existsByDni`. **Sin cambio de runtime:** el `DataIntegrityViolationException` ya lo mapea el `GlobalExceptionHandler` a **409** con mensaje limpio (no 500). **Verificado en vivo** contra la BD de dev: el índice de `telefono` crea limpio (0 dupes) y el de `dni` rechaza correctamente el dupe de test `60802327` ignorando placeholder/NULL. En prod (BD nueva desde baseline) no hay dupes. `V9__unique_telefono_dni.sql`. ⚠️ Si algún día migras una BD **con** dupes, resolverlos antes (prod = BD fresca, no aplica).
- [~] **U3 · Flujo de aprobación del ARRENDADOR** (opción a: admin aprueba documentos). ✅ HECHO el núcleo: `Arrendador.verificado` real (+ migración `V8`), recálculo KYC generalizado a arrendadores en `DocumentoService` (mismos docs requeridos DNI), eventos aprobación/rechazo **con rol** → notificación adaptada (arrendador: "ya puedes publicar" + link landlord), y `mapArrendadorInfo`/`mapArrendadorPublico` usan el flag real (antes fingían `estado==ACTIVE`). usuarios + mensajería compilan. **No se bloqueó la publicación** a propósito (era el comportamiento actual — el flag es un badge de confianza, no un gate; evitar romper). ⏳ Pendiente aparte: arrendadores de Google sin perfil, y re-test de la migración con Docker arriba (es un `ADD COLUMN` trivial + baseline ya verificado).
- [x] **U4 · Faltan estados REJECTED y SUSPENDED** en `EstadoUsuario` (solo PENDING/ACTIVE/BANNED). ✅ Agregados `REJECTED` (registro/KYC rechazado) y `SUSPENDED` (suspensión temporal) + helper `bloqueaAcceso()`. **Y se cerró el agujero real:** el login **no verificaba `estado`** → un usuario **BANEADO podía entrar igual**. Ahora `login`, `login-admin` y `google-login` rechazan (403) las cuentas baneadas/suspendidas/rechazadas (`verificarCuentaHabilitada`, tras validar password → no cuenta como fallo). **Escalada de privilegios cerrada:** `PUT /usuarios/{id}` permitía `esPropioUsuario`, y `actualizarUsuario` copiaba `estado` del body → un usuario podía **auto-activarse / quitarse el baneo**; ahora sólo un admin (`EDITAR_USUARIO`) puede cambiar `estado` (se ignora en auto-edición). Migración `V10` amplía el CHECK `usuarios_estado_check` a los 5 valores (el baseline sólo permitía 3 → sin esto persistir el nuevo estado fallaba). Frontend: type union + badges (`profile.ts`, `admin-user-service.ts` con `suspenderUsuario`/`rechazarUsuario`, `landlord/profile`, `UserDirectoryTable`, `duplicates`). **Verificado en vivo (app corriendo):** (1) login de cuenta SUSPENDED → **403** con el mensaje exacto → revert → 200; (2) un estudiante hizo `PUT` sobre su propio perfil: `nombre` **sí** cambió (200) pero `estado` quedó en PENDING (escalada **bloqueada**); (3) el CHECK ampliado aplica en dev y `SUSPENDED`/`REJECTED` persisten. usuarios compila; frontend typecheck en verde. `EstadoUsuario.java`, `AuthController.java`, `UsuarioController.java`, `V10__usuarios_estado_rejected_suspended.sql`.
- [x] **U5 · Cambiar contraseña/reset no revoca sesiones** y el token de reset era reutilizable. ✅ Nuevo `SesionService.revocarTodas()`; `reset` y `cambiarPassword` ahora revocan **todas** las sesiones (mata sesiones de un atacante). El token de reset es **de un solo uso** (se blacklistea tras usarlo; se rechaza si se reintenta). `PasswordResetService.java`, `UsuarioService.java`. Compila.
- [x] **U6 · Cambiar teléfono no limpia `telefonoVerificado`** → el badge "verificado" pasaba al número nuevo. ✅ Al cambiar el teléfono (perfil propio y admin), si el número **cambia** se pone `telefonoVerificado=false` (re-verificación por OTP). Si es el mismo, no toca el flag. `UsuarioController.java`, `UsuarioService.java`. Compila.
- [x] **U7 · Se serializa el hash de la contraseña** en `listarTodos()`/`obtenerPorId`. ✅ Añadido `@JsonIgnore` al campo `password` en `Usuario.java` (verificado: ningún endpoint recibe `Usuario` como body ni se cachea en Redis, así que no rompe login/registro).

---

## 🟡 INFRA / KAFKA / OBSERVABILIDAD

- [x] **I1 · Kafka sin volúmenes** (dev y prod) → se pierden topics/offsets al recrear. ✅ Agregados volúmenes persistentes a **zookeeper** (`zookeeper_data`+`zookeeper_log`) y **kafka** (`kafka_data`) en ambos compose. Validado con `docker compose config`. ⚠️ Toma efecto al recrear los contenedores (`docker compose up -d`); la primera recreación arranca con volumen vacío (igual que cualquier reinicio hoy), y de ahí en más persiste.
- [x] **I2 · Consumidores Java sin DLQ** — mensaje envenenado/fallo se descartaba en silencio. ✅ `DefaultErrorHandler` + `DeadLetterPublishingRecoverer` (4 intentos → `<topic>.DLT`) en **mensajería** y **propiedades** (bean auto-aplicado) y en **usuarios** (seteado en su factory custom). Los consumidores con DLQ manual propio (PagoEventListener, RefundEventListener) no se afectan. 3 servicios compilan. ⏳ Verificación runtime con Kafka arriba pendiente.
- [x] **I3 · Prometheus expuesto pero inservible** — ✅ `micrometer-registry-prometheus` en los 6 servicios (metrics live). **Y desplegado el stack:** nuevo `docker-compose.observability.yml` + `monitoring/` con **Prometheus** (:9090, scrape de `/actuator/prometheus` de los 5 servicios + usuarios vía Eureka SD + Zipkin) y **Grafana** (:3001, datasources + dashboard JVM/HTTP auto-provisionados). Métricas verificadas contra el `/actuator/prometheus` real (nada inventado). `docker compose config` + `promtool check` en verde. Compone sin tocar el stack: `docker compose -f docker-compose.yml -f docker-compose.observability.yml up -d`.
- [x] **I4 · Sin centralización de logs ni alertas.** ✅ En el mismo stack: **Loki** (:3100, retención 7d) + **Promtail** (logs de todos los contenedores Docker vía socket) → datasource en Grafana. **Alertas** en `monitoring/prometheus/alerts.yml`: servicio caído (`up==0`), tasa de 5xx alta, heap >90%, logs ERROR frecuentes (la de lag de Kafka queda de plantilla comentada porque no se pudo confirmar el nombre exacto de la métrica en vivo). `promtool check rules` OK. ⏳ Alertmanager (routing email/Slack) queda como follow-up documentado.
- [x] **I5 · Sin límites de memoria/CPU** en los contenedores → una fuga OOM-kill a los vecinos, y **peor**: las JVMs con `MaxRAMPercentage=75%` sin límite creían tener el 75% de TODO el host. ✅ Agregado `mem_limit` a los 9 servicios JVM/Node en `docker-compose.prod.yml` (usuarios/propiedades/pagos/mensajeria/notificaciones 1g; gateway/catalogos 768m; discovery/config 512m). BD/Kafka/Redis se dejaron sin cap a propósito (gestionan su memoria; limitarlas arriesga perf/OOM). Validado con `docker compose config`. ⏳ Ajustar valores a la RAM real del servidor cuando lo tengas (suma ≈7.5 GB solo estos 9 → recomendable host ≥16 GB, o baja los valores).
- [x] **I6 · WhatsApp re-escanea QR en cada reinicio** — la sesión se escribía en `/app/.wwebjs_auth` (CWD) pero el volumen se monta en `/home/app/.wwebjs_auth`. ✅ `LocalAuth({ dataPath: WWEBJS_DATA_PATH })` env-driven; en prod `WWEBJS_DATA_PATH=/home/app/.wwebjs_auth` (coincide con el volumen → sesión persiste). Dev sin la var → default de siempre. `node --check` + `compose config` OK.
- [x] **I7 · El consumidor Kafka de notificaciones y el `ready` de WhatsApp.** ✅ Arreglado el bug real: en cada reconexión de WhatsApp se creaba **otro** consumidor sin parar el anterior (fuga en el mismo grupo) → ahora arranca **una sola vez** y solo se marca activo tras un `start()` exitoso (si falla, el próximo `ready` reintenta). El "pile-up si nunca autentica" queda mitigado porque Kafka **retiene** los eventos hasta que WhatsApp vuelve (desacoplar del todo causaría fallos de envío durante el QR, así que no se hizo a propósito). `index.js`. `node --check` OK.
- [x] **I8 · Hashes bcrypt del seed son falsos** → el admin sembrado no puede loguear. ✅ RESUELTO mejor: se eliminó el `data.sql` roto (y `extra_permissions.sql` redundante) y se añadió **bootstrap del primer admin por env** en `DataInitializer` (`ADMIN_BOOTSTRAP_EMAIL`/`ADMIN_BOOTSTRAP_PASSWORD`, hash en runtime, sin secretos en git). Wiring en `servicio-usuarios.yml`, `docker-compose.prod.yml`, `.env.example`. Compila.

---

## 🟡 FRONTEND

- [x] **F1 · WebSocket sin fallback de producción** → chat/notificaciones se rompían vía ngrok/prod. ✅ `stomp-client.ts` ahora resuelve la URL igual que `api.ts`: en dev usa `ws://localhost:8086`, y en prod/ngrok (página no-localhost) usa **mismo-origen `wss://<host>/ws-mensajeria`** (nginx lo enruta a :8086). Funciona en dev Y en ngrok sin romper ninguno. Typecheck del front en verde.
- [x] **F2 · Re-suscripción rota tras reconexión** → chat/notificaciones dejaban de actualizar tras un corte. ✅ Ambos hooks (`use-notifications`, `use-stomp-chat`) ahora registran **siempre** `onConnect` (dispara al conectar y en cada reconexión), en vez de solo cuando no estaba conectado al montar. Typecheck en verde.
- [x] **F3 · shells vacíos del admin-master en el menú** → ✅ Ocultos del `AdminSidebar` los 12 links a páginas placeholder (metrics/heatmap|network|system, properties/history, reports/pending|active-bans, catalog/zones/prices, marketing/*, finance/payouts|invoices, system/audit) + eliminada la sección MARKETING (era 100% placeholder) y consolidados los grupos de un solo item. Verificado que las páginas conservadas son reales. Los archivos siguen existiendo (solo salen del menú). Typecheck en verde. `AdminSidebar.tsx`
- [x] **F4 · El home público cae en propiedades mock** que enlazaban a `/property/[id]` → 404 en prod. ✅ Quitado el `fallbackDestacados()` (mocks) del home: si el backend está vacío o falla, se muestra vacío en vez de propiedades falsas. Removidos los imports `MOCK_PROPIEDADES`/`distanciaAUpeuKm` sin uso. Typecheck en verde. `src/app/(public)/page.tsx`
- [x] **F5 · Sin SEO/Open Graph** → ✅ Agregados `robots.ts`, `sitemap.ts`, `manifest.ts` (PWA) en `src/app`. Y **OG por propiedad** vía un `layout.tsx` server-component hermano con `generateMetadata` (título/descr/imagen desde el backend) — **sin refactorizar** la página cliente (se envuelve transparente); fetch con timeout 3s + fallback, cacheado 5 min. Leí los docs de Next 16 antes (params es Promise). Typecheck en verde. ⏳ Setear `NEXT_PUBLIC_SITE_URL` al dominio real para URLs canónicas correctas.
- [~] **F6 · Dark mode inconsistente + dos design systems.** ✅ Núcleo hecho: el área de contenido de admin/landlord ya no se queda en claro en dark (hex hardcodeado → tokens `bg-background`/`text-foreground`/`text-muted-foreground` en `admin-master/layout.tsx` y `landlord/layout.tsx`); **`next-themes` (dep muerta) eliminado** y el `sonner`/toast ahora sigue el theme real del store Zustand (antes ignoraba el toggle). tsc + `npm run build` en verde, dev server sano. ⏳ A propósito NO se migraron los `legacy-*` (usados en 24 archivos, ya usan tokens, no hex) ni se repintó el chrome navy de los sidebars (flip a blanco en light = rediseño visual sin QA), ni un barrido página-por-página de colores claros. `sonner.tsx`, layouts.

---

## 🟡 TESTS

- [~] **T1 · Tests de integración** (Testcontainers con Postgres real). ✅ Arrancado: `WebhookPagoIntegrationTest` (pagos) y `DoubleBookingIntegrationTest` (propiedades, overlap de reservas), etiquetados `@Tag("integration")` + `@Testcontainers` — corren aparte del `mvn test` offline (necesitan Docker). ⏳ Falta cubrir la saga completa end-to-end.
- [x] **T2 · Tests de auth/OTP/lockout/RBAC** en usuarios. ✅ **18 tests unitarios puros** (JUnit5+Mockito, sin contexto Spring/BD): `EstadoUsuarioTest` (gate de acceso de U4), `PasswordResetServiceTest` (token de un solo uso + revocación de sesiones de U5, anti-enumeración), `RefundServiceTest` (idempotencia PAGADO→REEMBOLSADO + marcado de fallo de B3). Todos pasan.
- [x] **T3 · CI corre `mvn test` sin Postgres/Kafka.** ✅ RESUELTO y **verificado en vivo**: cada servicio (config-server, discovery-server, api-gateway, usuarios, propiedades, pagos, catalogos, mensajería) tiene `src/test/resources/application.yml` con H2 en memoria + Config Server/Eureka/Kafka apagados en tests, y el `pom.xml` de cada uno excluye `@Tag("integration")` del `mvn test` por defecto (surefire `excludedGroups`). **Corrí `mvn test` en los 8 módulos sin ninguna infra Docker corriendo (solo procesos java del stack, sin Postgres/MySQL/Kafka de test): los 8 → EXIT 0.** `.github/workflows/ci.yml` ya no necesita provisionar servicios (aunque seguirlos dejando no rompe nada).
- [x] **T4 · Smoke test end-to-end.** ✅ `scripts/smoke-test.sh` + `.ps1`: gateway health, búsqueda pública, login admin — falla con exit≠0 si algo no responde. **Verificado en vivo contra el stack real: las 3 verificaciones pasan.**

---

## 🔵 PARA QUE BRILLE — Producto

- [x] **G1 · Payout al arrendador.** ✅ Hoy el arrendador **nunca cobraba** — `montoArrendador` se calculaba (por pago) pero no se desembolsaba a nadie, sin visibilidad ni trazabilidad de cuánto se le debía. Resuelto con un **libro contable de payouts + flujo admin-mediado**:
  - `pagos` gana `arrendador_id` (capturado de `ReservaDetalleDTO.arrendadorId` — **ya venía en el JSON de propiedades**, sólo hubo que declarar el campo en el DTO del lado de pagos, cero cambios en propiedades) y `desembolso_id` (qué desembolso ya cubrió este pago, evita pagarlo dos veces).
  - Nueva tabla `desembolsos` (V2) + `DesembolsoService`: `resumenPendiente()` agrega por arrendador el `montoArrendador` de pagos `PAGADO` aún no cubiertos; `generar(arrendadorId)` crea el desembolso y **estampa** los pagos incluidos (atómico); `marcarProcesado(...)` (idempotente) registra método+referencia de la transferencia real hecha *fuera* de la app; `marcarFallido(...)` **libera** los pagos para reintentar (no se pierden en el limbo).
  - Endpoints: admin `GET/POST /pagos/admin/desembolsos/**` (`hasRole('ADMIN')`, dos capas: SecurityConfig + `@PreAuthorize`) + self-service `GET /pagos/arrendador/desembolsos` (el arrendador ve su propio historial, transparencia).
  - **Decisión deliberada — NO se automatizó con la API de Marketplace/Payouts de MercadoPago:** eso requiere que cada arrendador conecte su propia cuenta MP vía OAuth (onboarding que no existe hoy) y **mueve dinero real** — no es algo para automatizar de forma autónoma sin que el dueño del negocio lo autorice y pruebe con credenciales reales. Este v1 (admin genera → transfiere por fuera, banco/Yape/etc. → marca procesado con referencia) ya resuelve el problema real: **visibilidad y trazabilidad de lo que se debe**, en vez de que el arrendador simplemente nunca cobre y nadie lo note. Migrar a payout automático vía MP queda documentado como el siguiente paso.
  - **Verificado en vivo, TODO el ciclo, contra datos reales de la BD (con limpieza después):** pendientes agregados correctamente por arrendador (`{"1":2348.98,"2":750.00}`) → generar crea el desembolso PENDIENTE y **estampa** los 2 pagos cubiertos → arrendador desaparece de pendientes → `marcar-procesado` (método+referencia+admin que lo procesó, idempotente) → reintentar generar sin saldo → **409 correcto**. Y el camino de fallo: generar → `marcar-fallido` → **el pago se libera** (`desembolso_id` vuelve a NULL) → el arrendador **reaparece** en pendientes con el monto completo, listo para reintentar. `Desembolso.java`, `DesembolsoService.java`, `DesembolsoController.java`, `DesembolsoRepository.java`, `PagoRepository.java`, `Pago.java`, `ReservaDetalleDTO.java`, `PagoService.java`, `V2__desembolsos.sql`.
  - ⏳ El pago **grupal** (cuotas, `#38`) no captura `arrendadorId` (su DTO `CuotaPagoDTO`/endpoint `gruposClient.miCuota` no lo trae) → esos pagos quedan fuera de la agregación de payouts por ahora; flag para un follow-up si el flujo grupal gana volumen. Pagos anteriores a este cambio también tienen `arrendador_id=NULL` (no hay backfill retroactivo automático — decisión deliberada de no tocar datos históricos sin que un admin lo apruebe explícitamente).
- [~] **G2 · Renta mensual recurrente con recordatorios** (base). ✅ Nueva tabla `cuotas_renta` (V3: reserva_id, número, período, monto, estado PENDIENTE/PAGADA/VENCIDA, vencimiento) + `CuotaRentaService.generarCuotas` (idempotente) + `@Scheduled` que marca vencidas y emite evento de recordatorio (reutiliza el outbox) para cuotas próximas a vencer sin duplicar envíos. Endpoint `GET /reservas/{id}/cuotas` (dueño/admin) + generación admin. **Verificado en vivo:** tabla creada, propiedades/gateway arrancan con el scheduler activo. `CuotaRenta*`, `V3__cuotas_renta.sql`. ⏳ Es el **cronograma + recordatorios**, no el cobro mensual real (ese engancha con servicio-pagos, sub-item de G1) ni el consumidor en notificaciones — flagueado como sig. paso.
- [x] **G3 · Garantía / depósito** (captura, retención y devolución). ✅ Nueva tabla `depositos` (V3) + `DepositoService`: `crear(reservaId, monto)` (captura `arrendadorId` real vía el mismo Feign a propiedades que ya usa el resto del servicio), `capturar(id, paymentId?)` (PENDIENTE→RETENIDO, idempotente), `devolver(id)` (RETENIDO→DEVUELTO — si hay `paymentId` real de MP hace un **reembolso real** reutilizando el patrón exacto de `RefundService`; si no, sólo registra el estado porque se cobró por fuera), `retenerParcial(id, monto, motivo)` (reembolso parcial real vía `PaymentRefundClient.refund(id, monto)` si aplica) y `marcarPerdido(id, motivo)` (el arrendador se queda el 100%, con motivo auditado). Transiciones a estado terminal bloqueadas si ya no está `RETENIDO` (409). Endpoints admin-only `/pagos/admin/depositos/**` (misma decisión deliberada que G1: **no se automatizó el cobro** por MercadoPago tocando el webhook de pagos — ese es un flujo de dinero real ya probado, separar el cargo del depósito es un cambio de mayor riesgo que no se hizo a ciegas; hoy se cobra junto con la renta o por canal externo, y un admin lo captura). **Verificado en vivo, el ciclo completo:** crear (arrendadorId real capturado por Feign) → capturar → devolver completo → reintentar devolver (idempotente, 200) → segundo depósito → retener parcial (150 de 400 → devuelve 250, matemática correcta) → intentar transición inválida sobre estado terminal → **409 correcto** → probé también el camino de error (paymentId corrupto no numérico) → **falla limpio con 409 y mensaje claro, el servicio sigue vivo** (no se cae). `Deposito.java`, `DepositoService.java`, `DepositoController.java`, `DepositoRepository.java`, `V3__depositos.sql`.
- [x] **G4 · Generación de contrato** (PDF + aceptación electrónica). ✅ Nuevo `ContratoService` (PDFBox): `GET /reservas/{id}/contrato` genera un PDF con las partes (nombre del estudiante + arrendador/nombre comercial vía `UsuariosClient` Feign ya existente), el inmueble, términos (fechas, monto, período) y el estado de aceptación de cada parte — sólo para reservas ya acordadas (`APROBADA`/`PAGADA`/`FINALIZADA`; una `SOLICITADA` no tiene contrato aún, 409). `POST /reservas/{id}/contrato/firmar` registra la **aceptación electrónica** (timestamp por rol, idempotente) — documentado con honestidad: es un timestamp de aceptación desde cuenta autenticada, **no firma criptográfica/DocuSign** (eso queda de trabajo futuro si se necesita valor legal fuerte). Migración `V4__contrato_firmas.sql` (columnas nullable). Mismo control de acceso que ver la reserva (`obtenerPropia`: dueños o admin). **Encontrado y arreglado un bug real preexistente al probarlo:** `validarParticipante`/`validarGestorReserva` chequeaban `perfilId == null` **antes** del bypass de ADMIN → un admin **nunca** había podido ver (`GET /reservas/{id}`), editar ni eliminar ninguna reserva (el admin no tiene `perfilId`). Reordenado el check en ambos métodos. **Verificado en vivo:** admin ahora sí ve `GET /reservas/33` (antes 409); `GET .../contrato` → PDF válido (magic bytes `%PDF`, 1417 bytes); reserva `SOLICITADA` → 409 correcto; `firmar` como admin (no es parte) → 409 correcto. **Encontrado y arreglado un bug de encoding real:** Helvetica (Standard14 de PDFBox) no garantiza tildes/eñes/símbolos (—,·,º) — el primer PDF generado salía con caracteres corruptos en un extractor de texto real (`pdftotext`); se agregó un normalizador a ASCII seguro (NFD + reemplazos) aplicado a **todo** el texto del PDF — texto final verificado **100% limpio, cero caracteres corruptos**. `ContratoService.java`, `ReservaController.java`, `ReservaService.java`, `Reserva.java`, `pom.xml` (pdfbox 3.0.3), `V4__contrato_firmas.sql`.
- [x] **G5 · Reconciliación con MercadoPago.** ✅ Nuevo `ReconciliacionService` (`@Scheduled`, cada 15 min configurable + toggle): re-consulta a MP (`PaymentClient.get`) los pagos en `PENDIENTE_REVISION`/`DISCREPANCIA` (batch, antiguos primero) y concilia el estado local **reusando los mismos emisores del webhook + outbox** (approved→PAGADO, rejected→RECHAZADO, refunded→REEMBOLSADO), idempotente, tolera `MPApiException` por pago sin abortar el batch, `paymentId` nulo/no-numérico → revisión manual (ERROR). Endpoint admin on-demand `POST /pagos/admin/reconciliar/{reservaId}`. El re-encolado de outbox varado ya lo cubría **P2** (no duplicado). Compila; estados reales verificados en la BD. `servicio-pagos`.
- [~] **G6 · Búsqueda geoespacial "cerca de mí"** (radio/bounding-box). ✅ Backend: `GET /api/v1/propiedades/buscar/cerca?lat&lng&radioKm` (Haversine sobre SQL nativo, sin PostGIS; devuelve `distanciaKm`), índice `(latitud,longitud)`. ✅ **Frontend conectado**: la UI de "Cerca de mí" (botón, permiso de geolocalización, manejo de error) **ya existía en la rama pero hacía sorting client-side FALSO sobre `/buscar` normal** — nunca llamaba al endpoint real ni mostraba distancia real. Ahora `servicioPropiedades.buscarCerca(...)` llama al endpoint de verdad (radio fijo 5km — no había un selector de radio existente que reusar) y la tarjeta de propiedad muestra "a X km de ti" cuando el resultado viene de esa búsqueda. Aditivo: `obtenerPaginadas` sólo enruta a `buscarCerca` cuando hay coordenadas del usuario, si no seguía el camino de `/buscar` de siempre. **Verificado en vivo:** `tsc`/`build` en verde, curl contra el endpoint real confirma 6 resultados con `distanciaKm` ascendente (0.17→0.82). ⏳ Falta: búsquedas guardadas/alertas (no se abordó).
- [x] **G7 · Refresh tokens** y auto-renovación de sesión. ✅ Nuevo `RefreshTokenService` (Redis, igual patrón que `SesionService`/reset-password): opaco, se guarda sólo su **hash SHA-256**, TTL 30 días (`JWT_REFRESH_EXPIRATION`), y se **ROTA en cada uso** — el usado queda inválido de inmediato, así que reusar un refresh token robado después de que el legítimo ya rotó se **rechaza y delata el robo** en vez de permitirlo. `POST /auth/refresh` canjea un refresh token vigente por un access token nuevo sin pedir credenciales (ni requiere el access token viejo). `login`/`login-admin`/`google-login` unificados sobre `emitirSesion` (de paso, `login-admin` ganó registro de sesión, que antes le faltaba). `logout` revoca el refresh token si se lo mandan; cambio/reset de contraseña y baja de cuenta revocan **todos** los refresh tokens del usuario (mismo gancho que ya revocaba sesiones). Frontend: `auth-service.ts` guarda el refresh token en cookie (30 días) junto al access token, `cerrarSesion` ahora sí llama al backend (antes sólo limpiaba localmente — el JWT quedaba vivo tras "cerrar sesión"), y el interceptor de `api.ts` intenta **un refresh silencioso ante un 401** y reintenta la petición antes de expulsar al usuario (antes: cualquier 401 = fuera al toque). **Verificado en vivo, las 6 pruebas:** login emite ambos tokens → refresh rota ambos (valores nuevos confirmados) → **reusar el refresh token viejo rotado → 403 (detecta robo)** → el access token nuevo funciona → logout revoca el refresh token → reusarlo tras logout → 403. usuarios + frontend compilan/typecheck. `RefreshTokenService.java`, `AuthController.java`, `UsuarioService.java`, `PasswordResetService.java`, `AuthResponse.java`, `auth-service.ts`, `api.ts`, `otp-step.tsx`, `email-code-step.tsx`.
- [x] **G8 · Borrado de cuenta / GDPR + audit log.** ✅ **Audit log:** nueva tabla `audit_log` (V11) + `AuditLogService` (actor resuelto del JWT vía `registrarActorActual`), cableado en los 3 puntos que importan: cambio de `estado` por admin (suspender/banear/rechazar/activar), `DELETE /usuarios/{id}`, y verificación KYC de documentos. Endpoint de sólo-lectura `GET /usuarios/admin/audit` (paginado, filtros). **Verificado en vivo:** un admin suspendió un usuario de prueba → `audit_log` registró `CAMBIO_ESTADO_USUARIO | Usuario | 14 | nuevo estado=SUSPENDED | admin@gmail.com` correctamente. **Borrado/GDPR:** `DELETE /usuarios/me` (auto-servicio) y admin `DELETE /usuarios/admin/{id}` **anonimizan** (no hard-delete, evita huérfanos cross-servicio): PII borrada, sesiones revocadas, JWT actual blacklisteado, columna `eliminado` (V12) filtra del listado. **Login/google-login rechazan cuentas eliminadas** (mismo gate de U4). usuarios compila. `AuditLog.java`, `AuditLogService.java`, `UsuarioController.java`, `DocumentoController.java`, `V11__audit_log.sql`, `V12__usuarios_soft_delete.sql`. ⏳ Cambio de email/teléfono con re-verificación: teléfono ya lo hace U6; email queda pendiente.

## 🔵 PARA QUE BRILLE — Diseño / Operación

- [x] **D1 · PWA / experiencia móvil instalable.** ✅ `manifest.ts` ya existía; añadido `public/sw.js` (service worker conservador: precache del app-shell, network-first en navegación con fallback offline, cache-first para `/_next/static`; **nunca cachea `/api/` ni requests con `Authorization`**, sólo intercepta GET del mismo origen → backend/WS/Cloudinary pasan directo) + registro `service-worker-register.tsx` **sólo en producción** (dev HMR intacto) + header no-cache para `sw.js` en `next.config.ts`. tsc + build OK. ⏳ Íconos 192/512 PNG para install fiable en Android (falta generar el asset binario).
- [ ] **D2 · i18n real** (next-intl andamiado, 99% del texto hardcodeado en español). ⏸️ **Deferido a propósito:** la app es sólo-español hoy (multi-universidad sigue en español), así que el valor inmediato es bajo y el barrido es enorme y toca cientos de archivos (alto riesgo de romper el build para poco beneficio ahora). Se hace cuando se agregue un 2º idioma.
- [x] **D3 · Pulido visual final.** ✅ Retirados los 5 assets default de Next sin uso (`next.svg`, `vercel.svg`, `file/globe/window.svg`, grep-confirmado). ⏳ **Skip a propósito (sin cambios):** los `<img>` restantes son user-content remoto (Cloudinary/Google) o `blob:` de previews (next/image no soporta blob y necesita layout con QA visual).
  - ✅ **Refactor de `properties/add/page.tsx`** (2166 líneas → 834 en el archivo principal): dividido en 10 archivos hermanos siguiendo el patrón ya usado en el repo (`student/profile/*-tab.tsx`, sin carpeta `_components/`, que no existe en esta base): `property-form-icons.ts` (mapeo FA→Material), `property-form-types.ts` (`FormState`/`Errores`/constantes), `property-form-primitives.tsx` (`Section`/`Field`/`InputField`/`CustomSelect`/`ChipsMultiselect`/`Switch`/`CustomItemInput`/`SkeletonChips`), 5 archivos de sección (`info-basica-section.tsx`, `ubicacion-section.tsx`, `detalles-section.tsx`, `servicios-section.tsx`, `reglas-section.tsx`) y 2 de sidebar (`fotos-sidebar.tsx`, `acciones-sidebar.tsx`). `page.tsx` queda como orquestador puro: todo el estado y los handlers (`validar`, `handleSubmit`, `guardarBorrador`, `programarPublicacion`, geocoding, manejo de imágenes) sin cambios, cableados a los hijos por props explícitas y tipadas. **Cero cambios de comportamiento**: cada bloque JSX se movió verbatim (no se reescribió markup), sólo se parametrizaron las referencias a estado como props.
  - ✅ **Servicios duplicados — mensajería:** `messaging-service.ts` era un wrapper que reenviaba a `conversationService` ("Reusa conversationService para no duplicar lógica HTTP", según su propio comentario) usado únicamente por `use-stomp-chat.ts` — y ese hook, a su vez, **no lo usaba nadie** (código muerto real, confirmado por grep). Eliminados los 3 archivos: `messaging-service.ts`, `use-stomp-chat.ts`, `types/messaging.ts` (shim de re-export de `types/chat.ts`, también sin otros consumidores). Riesgo cero: nada más los importaba.
  - ✅ **Servicios duplicados — reseñas, evaluado con cautela:** `reviews-service.ts` y `resena-service.ts` ambos llaman `GET /resenas/arrendador/{id}`, pero **no son duplicados reales** — tienen consumidores distintos con contratos distintos: `reviewsService.listarResenasArrendador` sólo lo usa la página de reseñas del arrendador (`landlord/messages/reviews/page.tsx`, tipo crudo `@/types/review`), `resenaService.getResenasPorArrendador` sólo lo usa el diálogo de reseña del estudiante (`review-form-dialog.tsx`, tipo normalizado vía `fromBackend()` con nombres de campo distintos, ej. `calificacion` en vez de `rating`). Fusionarlos exigía tocar dos flujos ya probados (reseñas que recibe el arrendador + el flujo en el que el estudiante reseña) por una ganancia puramente interna de DRY — mismo criterio de riesgo que ya aplicado en G1/G3/G2: no tocar código funcionando de dos features en producción sin beneficio real para el usuario. Se dejaron separados a propósito. Sí se eliminó `reviewsService.calificacionArrendador` (método muerto, cero consumidores, verificado por grep) y su import huérfano `CalificacionResumen`.
  - **Verificado:** `tsc --noEmit` limpio en todo el proyecto tras el refactor. `npm run build` (Next 16 + Turbopack) compila, tipa y **prerenderiza** las 76 rutas sin error, incluida `/landlord/properties/add`. `eslint` sobre los archivos nuevos sin issues nuevos (sólo warnings preexistentes idénticos al original, no introducidos). Dev server reiniciado limpio (`Ready` sin errores). ⚠️ **Honestidad sobre el alcance de la verificación:** no hay herramienta de navegador/Playwright disponible en este entorno, así que no se hizo un click-through real del formulario (llenar campos, subir foto, publicar) — la ruta está protegida por sesión y el middleware bloquea la carga sin cookie válida, así que tampoco se pudo forzar el render autenticado por curl. La verificación se apoya en: tipado estricto de cada prop entre los 10 archivos (cualquier desalineación de firma falla la compilación), movimiento verbatim del JSX (no reescritura), auditoría manual línea por línea del cableado de props, y que Next ya ejecuta el árbol de render completo de esta ruta durante `next build`. Se recomienda una prueba manual en el navegador (llenar y publicar una propiedad de prueba) antes de considerar esto 100% blindado.

---

## ✅ Lo que ya está bien (no tocar, solo referencia)

- Outbox transaccional + idempotencia de consumidores (`processed_events`).
- Saga persistida con compensación y scheduler de reintentos.
- Locking optimista (`@Version`) + pesimista (`findByIdForUpdate`) contra doble-booking.
- Resilience4j (circuit breaker/retry/bulkhead/timeout) en cada llamada Feign.
- Webhook MP con HMAC (comparación constante) + re-fetch del pago como fuente de verdad.
- Reseñas verified-stay (solo con reserva FINALIZADA) + agregación de rating.
- Tracing distribuido (Micrometer + Zipkin) sobre REST/Feign/Kafka.
- Chat feature-complete (persistencia, paginación, read receipts, typing, moderación, WS con handshake JWT).
- Consumidor Kafka de notificaciones con retry + backoff + DLQ.
- Frontend: protección de rutas por rol en profundidad, estados de carga/error/vacío reutilizables, validación Zod fuerte, mapas/carruseles/comparador/calendario ya construidos.
- Multi-universidad (Fase 1) ya migrado en catálogos.

---

_Generado por revisión completa multi-agente. Actualiza la tabla de progreso al marcar tareas._
